{
  "extends": "base.tsconfig.json",
  "include": ["${configDir}/src", "${configDir}/scripts"],
  "compilerOptions": {
    "tsBuildInfoFile": "${configDir}/.tsbuildinfo/scripts.tsbuildinfo",
    "rootDir": "${configDir}",
    "noEmit": true
  }
}
