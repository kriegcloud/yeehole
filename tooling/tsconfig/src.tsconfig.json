{
  "extends": "./base.tsconfig.json",
  "include": ["${configDir}/src"],
  "compilerOptions": {
    "types": ["node"],
    "outDir": "${configDir}/build/src",
    "tsBuildInfoFile": "${configDir}/.tsbuildinfo/src.tsbuildinfo",
    "rootDir": "${configDir}/src"
  }
}
